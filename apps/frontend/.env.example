# API base URL — set to your backend URL in production
VITE_API_BASE_URL=http://localhost:8000

# Run mode: "api" uses real backend, "mock" uses built-in demo data
# Set to "mock" if you want to run the frontend without a backend
VITE_RUN_MODE=api
