# Core agent framework
langgraph>=0.2.0,<0.3.0
langchain-core>=0.2.0,<0.4.0

# Gemini — use the native SDK directly (avoids region restriction issues in langchain-google-genai)
google-genai>=1.0.0

# Vector search
faiss-cpu>=1.8.0
numpy>=1.26,<2.0

# Sentence embeddings — commented out for Render free-tier deployment (DISABLE_NEURAL_SEARCH=true)
# Uncomment if running on an instance with >2GB RAM and neural search enabled
# sentence-transformers>=3.0.0

# TF-IDF fallback (ships with scikit-learn, no torch dependency)
scikit-learn>=1.4.0

# HTTP client for Gemini REST API calls
httpx>=0.27.0

# Data + utils
pandas>=2.0
python-dotenv
pydantic>=2.0

# Testing
pytest